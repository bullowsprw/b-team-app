
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.bullows.bteam',
    appName: 'B Team',
    webDir: 'out',
    server: {
        androidScheme: 'https'
    }
};

export default config;
